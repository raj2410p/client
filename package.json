{
  "name": "fullstackassignment",
  "private": true,
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client && npm install --prefix backend/src",
    "start": "node backend/src/server.js"
  }
}